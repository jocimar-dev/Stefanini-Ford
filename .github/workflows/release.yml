name: Release - Tag Build & Publish

on:
  push:
    tags:
      - "v*"
  release:
    types: [published]   # ao publicar Release na UI
  workflow_dispatch:     # permitir acionar manualmente

permissions:
  contents: read
  packages: write

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/todo-api
      VERSION: ${{ github.event.release.tag_name || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Build & test
        run: mvn -B -DskipTests=false clean verify

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    environment: production  # configure reviewers no ambiente "production"
    env:
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/todo-api
      VERSION: ${{ github.event.release.tag_name || github.ref_name }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push release image
        run: |
          docker buildx build \
            --platform linux/amd64 \
            -t $IMAGE_NAME:$VERSION \
            -t $IMAGE_NAME:latest \
            --push .
